import {
    expect,
    assert,
    should
} from 'chai';
import 'mocha';
import {Database} from "../src/Database";

const WORKING_DB_DIR : string = './db/test.db';

// TODO: rewrite the database tests.
describe('constructor', () => {

});